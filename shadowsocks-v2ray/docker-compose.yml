version: '3'

services:
  nginx:
    image: nginx:stable
    volumes:
      - "./nginx.conf:/etc/nginx/conf.d/default.conf"
      - "/etc/letsencrypt:/etc/letsencrypt"
    ports:
      - "443:443"
      - "80:80"
    restart: "always"

  ss-server:
    build:
      context: .
    environment:
      PASSWORD: "8b96!qEzL*u7"
    restart: "always"
